# Python dependencies for advanced audio analysis
librosa>=0.10.1
numpy>=1.26.0
scipy>=1.11.0
soundfile>=0.12.0
numba>=0.56.0

# 高度音声処理（オプション - 別途インストール）
# pip install crepe  # ピッチ検出
# pip install pyworld  # WORLD vocoder

# システム依存（外部インストール必要）
# FFmpeg: システムレベルでインストール
# Rubber Band: 高品質テンポ処理用